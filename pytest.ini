[pytest]
# Registro de marcas personalizadas (evita PytestUnknownMarkWarning)
markers =
    functional: tests that make real external calls (API, network, etc.)
    requires_api_key: tests that require OPENAI_API_KEY (or similar) to run
